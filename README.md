# My Portfolio

Personal portfolio site (React + CRA/CRACO).

## Setup

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

- `npm run dev` / `npm start` — local development server
- `npm run build` — production build
- `npm test` — run tests
